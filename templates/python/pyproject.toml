[project]
name = "PROJECT_NAME"
version = "0.1.0"
description = "DESCRIPTION"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = [
    "prek>=0.3.8",
    "pytest>=9.0.0",
    "ruff>=0.9.0",
]

[dependency-groups]
test = ["pytest>=9.0.0"]
lint = ["ruff>=0.9.0"]